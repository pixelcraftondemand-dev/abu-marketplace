# openai@5.x declares a peerOptional dependency on zod@^3.23.8, but this
# project uses zod@^4. npm fails with ERESOLVE on clean installs (CI/Vercel).
# Ignore peer dependency conflicts for installation.
legacy-peer-deps=true
